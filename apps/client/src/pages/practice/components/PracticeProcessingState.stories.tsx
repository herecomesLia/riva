import preview from "#storybook/preview"

import { PracticeProcessingState } from "./PracticeProcessingState"

const meta = preview.meta({
  component: PracticeProcessingState,
  title: "Practice/PracticeProcessingState",
})

export const Processing = meta.story({})
